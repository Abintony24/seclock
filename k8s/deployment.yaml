apiVersion: apps/v1
kind: Deployment
metadata:
  name: seclock
spec:
  replicas: 2
  selector:
    matchLabels:
      app: seclock
  template:
    metadata:
      labels:
        app: seclock
    spec:
      containers:
        - name: seclock
          image: 504483085764.dkr.ecr.us-east-1.amazonaws.com/seclock:5
          ports:
            - containerPort: 8080
